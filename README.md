# HomerUIKit

UIKit extensions and design tokens for the Homer Swift suite. Eliminates magic numbers and copy-pasted UIView setup boilerplate.

## Status

`v0.1.0` in development on the `develop` branch.
